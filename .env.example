# Analytics is Google Tag Manager, container id in content/business.ts.
# The container fires GA4 (G-BFR37L657V) and Google Ads (AW-18000649811)
# itself, so do NOT add a GA4 measurement id here — it would double-count.

# ⚠️ THIS FILE IS COMMITTED TO GIT. PLACEHOLDERS ONLY — NEVER REAL VALUES.
#
# It previously carried a working MongoDB password and a live Brevo API key,
# both of which reached the GitHub remote. Both have to be treated as
# compromised and rotated at the provider; deleting them from here does not
# un-publish them. Real values belong in .env.local (gitignored) locally and in
# the Netlify environment in production.

# MongoDB connection string. Lead capture only — all page content is in-repo.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/

# Brevo (transactional email) — lead notifications from the gate problem form.
# API key: https://app.brevo.com/settings/keys/api  (v3 keys start "xkeysib-")
BREVO_API_KEY=xkeysib-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Where lead notifications are delivered.
LEAD_NOTIFY_TO=office@shieldgaterepair.com

# Sender shown on the notification. The domain must be verified in Brevo
# (Senders, Domains & Dedicated IPs) or Brevo rejects the send.
BREVO_SENDER_EMAIL=office@shieldgaterepair.com
BREVO_SENDER_NAME=Shield Gate Repair Website

# Review request system
REVIEW_REQUEST_SECRET=
SMS_PROVIDER_KEY=

# ── Asset CDN ───────────────────────────────────────────────────────────────
# Images and videos are mirrored to S3 and served through CloudFront. Set this
# to route them there. Leave it unset and everything falls back to the local
# /public paths — a missing CDN degrades to a working site, not a broken one.
#
# Inlined at build time, so changing it needs a rebuild, not just a restart.
#
# ⚠️ MEASURED 6 Sep 2026: THIS IS NOT SET IN NETLIFY.
# The live HTML serves every image from the Netlify origin — `curl -s
# https://shieldgaterepair.com/ | grep -c cloudfront` returns 0. Mobile LCP is
# 6.1 s against a 2.5 s threshold, and PERFORMANCE-AUDIT.md puts this single
# variable at −1.5 to −3 s. Setting it here does nothing on its own: it has to
# be set in the Netlify UI (Site configuration → Environment variables) and the
# site redeployed. Verify with the curl above returning a non-zero count.
NEXT_PUBLIC_CDN_URL=https://d7t2eyla4jb9a.cloudfront.net

# Used only by scripts/sync-cdn.sh. AWS credentials are NOT read from this file
# — export them in your shell or use `aws configure`.
CDN_BUCKET=shield-gate-repair-assets
CDN_DISTRIBUTION_ID=EQLQJEJ0EPUUW

# Private, versioned bucket holding the source originals in client-assets/.
# Never served publicly and not fronted by CloudFront. It is the only backup of
# the .mov masters, which are too large for git.
MASTERS_BUCKET=shield-gate-repair-masters
